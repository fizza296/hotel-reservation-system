/** @type {import('next').NextConfig} */
const nextConfig = {};

module.exports = {
    images: {
      domains: ['images.trvl-media.com'],  // Add more domains as needed
    },
    // other configurations...
  };
